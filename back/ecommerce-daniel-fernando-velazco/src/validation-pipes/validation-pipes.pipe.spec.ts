import { ValidationPipesPipe } from './validation-pipes.pipe';

describe('ValidationPipesPipe', () => {
  it('should be defined', () => {
    expect(new ValidationPipesPipe()).toBeDefined();
  });
});
